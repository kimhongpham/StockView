# ==================================
# 1. C?u hình ?ng d?ng & Server
# ==================================
spring.application.name=${APPLICATION_NAME:backend}
server.port=${API_PORT:8080}
# server.port ???c ??i tên thành API_PORT cho rõ ràng trong ENV
# N?u b?n không ??i tên bi?n môi tr??ng, hãy gi? nguyên server.port=${SERVER_PORT:8080}

# ==================================
# 2. C?u hình Database (PostgreSQL)
# ==================================
# Xây d?ng URL t? các bi?n ENV (DB_SSL_MODE ?ã ???c s?a thành disable trong .env)
spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=${DB_SSL_MODE}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# C?u hình JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Kh?c ph?c l?i 'Unable to determine Dialect'
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


# ==================================
# 3. C?u hình Redis
# ==================================
spring.data.redis.host=${REDIS_HOST}
spring.data.redis.port=${REDIS_PORT}
spring.data.redis.password=${REDIS_PASSWORD}
spring.data.redis.database=${REDIS_DB}


# ==================================
# 4. C?u hình API Keys & JWT (L?y giá tr? b?ng @Value)
# ==================================
# Các bi?n này th??ng ???c l?y trong code Java b?ng @Value, không c?n ??nh ngh?a l?i
# ? ?ây n?u b?n mu?n gi? chúng hoàn toàn trong file .env

finnhub.api.key=${FINNHUB_API_KEY}
crypto.api.key=${CRYPTO_API_KEY}
metals.api.key=${METALS_API_KEY}

jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}
jwt.refresh-expiration=${JWT_REFRESH_EXPIRATION}

spring.security.user.name=admin
