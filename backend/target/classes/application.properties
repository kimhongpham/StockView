# ==================================
# 1. C?u h�nh ?ng d?ng & Server
# ==================================
spring.application.name=${APPLICATION_NAME:backend}
server.port=${API_PORT:8080}
# server.port ???c ??i t�n th�nh API_PORT cho r� r�ng trong ENV
# N?u b?n kh�ng ??i t�n bi?n m�i tr??ng, h�y gi? nguy�n server.port=${SERVER_PORT:8080}

# ==================================
# 2. C?u h�nh Database (PostgreSQL)
# ==================================
# X�y d?ng URL t? c�c bi?n ENV (DB_SSL_MODE ?� ???c s?a th�nh disable trong .env)
spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=${DB_SSL_MODE}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# C?u h�nh JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Kh?c ph?c l?i 'Unable to determine Dialect'
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


# ==================================
# 3. C?u h�nh Redis
# ==================================
spring.data.redis.host=${REDIS_HOST}
spring.data.redis.port=${REDIS_PORT}
spring.data.redis.password=${REDIS_PASSWORD}
spring.data.redis.database=${REDIS_DB}


# ==================================
# 4. C?u h�nh API Keys & JWT (L?y gi� tr? b?ng @Value)
# ==================================
# C�c bi?n n�y th??ng ???c l?y trong code Java b?ng @Value, kh�ng c?n ??nh ngh?a l?i
# ? ?�y n?u b?n mu?n gi? ch�ng ho�n to�n trong file .env

finnhub.api.key=${FINNHUB_API_KEY}
crypto.api.key=${CRYPTO_API_KEY}
metals.api.key=${METALS_API_KEY}

jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}
jwt.refresh-expiration=${JWT_REFRESH_EXPIRATION}

# ... v� c�c c?u h�nh ENV kh�c (Email, Telegram, n8n) t??ng t?.